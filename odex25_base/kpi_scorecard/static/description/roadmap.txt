Under consideration
--------------------
* manual (not compute actual value) for KPI target
* clear formula button
* choose period using select2 with search
* result representation - progress bar
* Block the menu for ordinary users as settings (assign for the menu the group editor)